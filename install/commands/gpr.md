---
description: Run one iteration of the gpr audit-verified loop in this session.
---

Activate the `gpr` skill (defined in `~/.claude/skills/gpr/SKILL.md`). Run exactly one iteration in this session, then yield control to the user. Do not auto-continue across iterations — the user types `/gpr` again when ready.
